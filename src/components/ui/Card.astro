---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`bg-white rounded-lg card p-6 text-primary ${className}`}>
  <slot />
</div>

<style>
  .card {
    box-shadow: 0px 0px 5px 2px rgba(41, 134, 174, 0.8);
  }
</style>
